// 1. Визначення типу геометричної фігури за допомогою switch:
// Створіть код, який приймає назву геометричної фігури ("коло", "квадрат", "трикутник") і
//  використовуючи switch оператор, виводить повідомлення про кількість сторін цієї фігури (наприклад, "Коло має 0 сторін",
//  "Квадрат має 4 сторони").

let shape = prompt('Введіть назву пласкої (двохмірної) геометричної фігути.');
switch (shape) {
    case 'коло':
    case 'Коло':
        alert('Коло має 0 кутів!');
        break;
    case  'трикутник':
    case  'Трикутник':
        alert('Трикутник має 3 кути!');
        break;
    case  'квадрат':
    case  'Квадрат':
        alert('Квадрат має 4 кути!');
        break;    
    case  'Прямокутник':
    case  'прямокутник':
        alert('Прямокутник має 4 кути!');
        break; 
    default: 
        alert('Щось пішло не так!'); 
        break;
}

// 2. Підрахунок суми парних чисел:
// Створіть код, який використовує цикл while для знаходження суми всіх парних чисел в 
// заданому діапазоні (наприклад, від 1 до 20).

let sum = 0;
let index = 1;
while (index <= 20) {
   if (index % 2 === 0) {
       sum += index;
   }
   index++;
}
console.log(`Сума парних чисел від 1 до 20 дорівнює ${sum}`);

// !!!ВАЖЛИВО!!! Вирішував задачцу не я а штучний інтелект.
// Це єдине задача, яку я не подужав.
// Ми не проходили такого матеріалу на наших уроках.

// 3. Таблиця множення:
// Використовуючи цикл for, напишіть програму, яка генерує "таблицю множення"
// виводимо в console.log операції  для конкретного числа (наприклад, для числа 5).

let nomber = prompt('Введіть число, на яке потрібна табличка множення');
for (let index = 1; index < 11; index++) {
     console.log (`Множимо на ${index} = ${index*nomber}`);
}

// 4. Зворотний лічильник:
// Використовуючи цикл for, напишіть програму, яка виводить числа від 10 до 1 в зворотньому 
// порядку.

for (let index=10; index>0; index--) {
  console.log (`${index}`)
}

// 5. Визначення чи є число парним чи непарним:
// Напишіть програму, яка приймає число і використовуючи if та for, 
// виводить повідомлення про те, чи є число парним чи непарним.
 
for (let index = prompt('Введіть число від 1 до 100'); index; index--) {
    if (index < 1 || index > 100) {
        console.log(`Ви ввели щось не те!`);
        break;
    }

    if (index % 2 === 0) {
        console.log(`Ваше число парне!`);
   } else {
       console.log(`Ваше число непарне!`);
   }
   if (index > 1 && index < 100) {
       break;
       }   
}

// 6. Знаходження першого парного числа:
// Напишіть програму, яка використовує цикл for для перебору чисел від 1 до 10 і
//  виводить перше знайдене парне число. Використайте оператор break, щоб припинити 
// виконання циклу, якщо парне число знайдено.

for (let index = 1; index < 11; index++) {
        if (index%2===0) {
        console.log (`${index}`);
        break;
    }
}

// 7. Виведення парних чисел:
// Створіть програму, яка використовує цикл for для перебору чисел від 1 до 10 і виводить тільки парні числа. 
// Використайте оператор continue, щоб пропустити ітерації для непарних чисел.

for (let index = 1; index < 11; index++) {
    if (index%2>0) {
        continue;
    };
 console.log (`${index}`);   
}